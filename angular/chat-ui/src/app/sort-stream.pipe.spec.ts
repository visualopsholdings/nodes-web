import { SortStreamPipe } from './sort-stream.pipe';

describe('SortStreamPipe', () => {
  it('create an instance', () => {
    const pipe = new SortStreamPipe();
    expect(pipe).toBeTruthy();
  });
});
